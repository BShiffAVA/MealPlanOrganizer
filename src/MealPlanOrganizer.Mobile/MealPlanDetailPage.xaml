<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MealPlanOrganizer.Mobile"
             xmlns:viewmodels="clr-namespace:MealPlanOrganizer.Mobile.ViewModels"
             xmlns:converters="clr-namespace:MealPlanOrganizer.Mobile.Converters"
             x:Class="MealPlanOrganizer.Mobile.MealPlanDetailPage"
             x:DataType="local:MealPlanDetailPage"
             Title="Meal Plan">
    <ContentPage.Resources>
        <converters:ActionButtonColorConverter x:Key="ActionButtonColorConverter"/>
        <converters:InvertedBoolConverter x:Key="InverseBoolConverter"/>
    </ContentPage.Resources>
    <Grid RowDefinitions="Auto,Auto,Auto,*" Padding="16" RowSpacing="12">
        
        <!-- Header with plan info -->
        <Frame
            Padding="16"
            CornerRadius="12"
            BackgroundColor="Black"
            BorderColor="#333333"
            HasShadow="False">
            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                <Label
                    Text="{Binding ViewModel.PlanName}"
                    FontSize="20"
                    FontAttributes="Bold"
                    TextColor="White" />
                <Frame
                    Grid.Column="1"
                    Padding="8,4"
                    CornerRadius="12"
                    HasShadow="False"
                    BackgroundColor="{Binding ViewModel.StatusColor}">
                    <Label
                        Text="{Binding ViewModel.Status}"
                        FontSize="12"
                        TextColor="White" />
                </Frame>
                <Label
                    Grid.Row="1"
                    Grid.ColumnSpan="2"
                    Text="{Binding ViewModel.DateRange}"
                    FontSize="14"
                    TextColor="White"
                    Margin="0,4,0,0" />
            </Grid>
        </Frame>

        <!-- Progress section -->
        <Frame
            Grid.Row="1"
            Padding="12"
            CornerRadius="8"
            BackgroundColor="#E3F2FD"
            BorderColor="Transparent"
            HasShadow="False">
            <HorizontalStackLayout Spacing="12">
                <Label
                    Text="{Binding ViewModel.ProgressText}"
                    FontSize="14"
                    TextColor="#1976D2"
                    VerticalOptions="Center" />
                <ProgressBar
                    Progress="{Binding ViewModel.ProgressValue}"
                    ProgressColor="#4CAF50"
                    HorizontalOptions="FillAndExpand"
                    VerticalOptions="Center" />
            </HorizontalStackLayout>
        </Frame>

        <!-- Drag-drop hint -->
        <Label
            Grid.Row="2"
            Text="💡 Long-press and drag recipes between days to reorganize"
            FontSize="12"
            TextColor="Gray"
            HorizontalOptions="Center"
            IsVisible="{Binding ViewModel.ShowDragDropHint}" />

        <!-- Loading Indicator -->
        <ActivityIndicator
            Grid.Row="3"
            IsRunning="{Binding ViewModel.IsLoading}"
            IsVisible="{Binding ViewModel.IsLoading}"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            Color="#6C63FF" />

        <!-- Days List -->
        <CollectionView
            x:Name="DaysCollection"
            Grid.Row="3"
            ItemsSource="{Binding ViewModel.Days}"
            IsVisible="{Binding ViewModel.IsLoading, Converter={StaticResource InverseBoolConverter}}"
            SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:MealPlanDayViewModel">
                    <Frame
                        x:Name="DayFrame"
                        Margin="0,0,0,8"
                        Padding="12"
                        CornerRadius="10"
                        BorderColor="{Binding DropTargetBorderColor}"
                        BackgroundColor="{Binding DropTargetBackgroundColor}"
                        HasShadow="False">
                        <Frame.GestureRecognizers>
                            <DropGestureRecognizer
                                AllowDrop="True"
                                DragOverCommand="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=DragOverCommand}"
                                DragOverCommandParameter="{Binding}"
                                DragLeaveCommand="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=DragLeaveCommand}"
                                DragLeaveCommandParameter="{Binding}"
                                DropCommand="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=DropCommand}"
                                DropCommandParameter="{Binding}" />
                        </Frame.GestureRecognizers>
                        <Grid ColumnDefinitions="Auto,*,Auto" RowDefinitions="Auto,Auto" ColumnSpacing="12">
                            
                            <!-- Day Info -->
                            <VerticalStackLayout
                                VerticalOptions="Center"
                                WidthRequest="60">
                                <Label
                                    Text="{Binding DayOfWeek}"
                                    FontSize="12"
                                    FontAttributes="Bold"
                                    TextColor="#6C63FF"
                                    HorizontalOptions="Center" />
                                <Label
                                    Text="{Binding DisplayDate}"
                                    FontSize="10"
                                    TextColor="Gray"
                                    HorizontalOptions="Center" />
                            </VerticalStackLayout>
                            
                            <!-- Recipe Info or Empty State -->
                            <Frame
                                Grid.Column="1"
                                Grid.RowSpan="2"
                                Padding="10"
                                CornerRadius="8"
                                BackgroundColor="Black"
                                BorderColor="#333333"
                                HasShadow="False"
                                VerticalOptions="Center"
                                Opacity="{Binding DragOpacity}">
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=ViewModel.ViewRecipeCommand}"
                                        CommandParameter="{Binding}" />
                                    <DragGestureRecognizer
                                        CanDrag="{Binding HasRecipe}"
                                        DragStartingCommand="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=DragStartingCommand}"
                                        DragStartingCommandParameter="{Binding}"
                                        DropCompletedCommand="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=DropCompletedCommand}"
                                        DropCompletedCommandParameter="{Binding}" />
                                </Frame.GestureRecognizers>
                                <Grid ColumnDefinitions="Auto,*">
                                    <!-- Recipe Image (if available) -->
                                    <Frame
                                        IsVisible="{Binding HasRecipeImage}"
                                        WidthRequest="50"
                                        HeightRequest="50"
                                        Padding="0"
                                        CornerRadius="8"
                                        IsClippedToBounds="True"
                                        HasShadow="False">
                                        <Image
                                            Source="{Binding RecipeImageUrl}"
                                            Aspect="AspectFill" />
                                    </Frame>
                                    
                                    <VerticalStackLayout
                                        Grid.Column="1"
                                        Margin="8,0,0,0"
                                        VerticalOptions="Center">
                                        <Label
                                            Text="{Binding RecipeTitle}"
                                            FontSize="14"
                                            FontAttributes="Bold"
                                            TextColor="White"
                                            LineBreakMode="TailTruncation" />
                                        <Label
                                            Text="{Binding RecipeDetails}"
                                            FontSize="11"
                                            TextColor="#CCCCCC"
                                            IsVisible="{Binding HasRecipe}" />
                                    </VerticalStackLayout>
                                </Grid>
                            </Frame>
                            
                            <!-- Action Button -->
                            <Button
                                Grid.Column="2"
                                Grid.RowSpan="2"
                                Text="{Binding ActionButtonText}"
                                Clicked="OnDayActionClicked"
                                CommandParameter="{Binding .}"
                                BackgroundColor="{Binding HasRecipe, Converter={StaticResource ActionButtonColorConverter}}"
                                TextColor="White"
                                Padding="12,8"
                                CornerRadius="8"
                                FontSize="12"
                                VerticalOptions="Center" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>

</ContentPage>
